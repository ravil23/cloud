syntax = "proto3";

package cfa;

service Analyzer {
  rpc analyze (Tick) returns (AnalyzerResult) {}
}

message Tick {
  int64 price = 1;
  int64 volume = 2;
}

message AnalyzerResult {
  string category = 1;
  string weight = 2;
}